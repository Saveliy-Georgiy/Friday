export type RegistrationActionsType = {}

const initialState = {}

export type AuthType = typeof initialState

export const registrationReducer = (state = initialState, action: RegistrationActionsType): AuthType => {
    return state
}