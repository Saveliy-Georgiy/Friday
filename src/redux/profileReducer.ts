export type ProfileActionsType = {}

const initialState = {}

export type AuthType = typeof initialState

export const profileReducer = (state = initialState, action: ProfileActionsType): AuthType => {
    return state
}